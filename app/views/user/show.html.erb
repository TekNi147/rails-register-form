<h1>Users</h1>
<ol>
  <% @users.each do |user| %>
    <li><%= user.name %></li>
    <%= button_to 'Delete', delete_user_path(user) , method: :delete, data: { confirm: 'Are you sure?'} %>
  </td>
<% end %>
</ol>
